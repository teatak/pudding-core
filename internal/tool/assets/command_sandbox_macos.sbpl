(version 1)

; Closed by default. Project and toolchain grants are appended per command.
(deny default)

; Child processes inherit the same sandbox.
(allow process-exec)
(allow process-fork)
(allow signal (target same-sandbox))
(allow process-info* (target same-sandbox))

(allow file-read* file-test-existence file-write-data
  (literal "/dev/null")
  (literal "/dev/zero"))
(allow file-read* file-test-existence
  (literal "/dev/random")
  (literal "/dev/urandom"))
(allow file-read-data file-test-existence file-write-data
  (subpath "/dev/fd"))
(allow file-read-metadata file-test-existence
  (literal "/etc")
  (literal "/tmp")
  (literal "/var")
  (literal "/private")
  (subpath "/private/var"))
(allow file-read* file-test-existence (literal "/"))

; Runtime services used by common CLI tools.
(allow mach-lookup
  (global-name "com.apple.PowerManagement.control")
  (global-name "com.apple.bsd.dirhelper")
  (global-name "com.apple.logd")
  ; Native TLS certificate evaluation (Go crypto/x509, Security.framework).
  ; This does not grant securityd access or access to private keychain files.
  (global-name "com.apple.trustd.agent")
  (global-name "com.apple.system.opendirectoryd.libinfo")
  (global-name "com.apple.system.opendirectoryd.membership"))
(allow ipc-posix-sem)

(allow sysctl-read
  (sysctl-name-prefix "hw.")
  (sysctl-name-prefix "kern.")
  (sysctl-name-prefix "machdep.cpu.")
  (sysctl-name-prefix "sysctl.proc_")
  (sysctl-name-prefix "vm."))

; Approved commands may use outbound networking. Listening remains loopback-only.
(allow network-bind (local ip "localhost:*"))
(allow network-inbound (local ip "localhost:*"))
(allow network-outbound)
